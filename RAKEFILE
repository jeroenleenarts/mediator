desc 'Publishing the website via rsync'
task :deploy do
  puts 'Publishing website, silence is golden...'
  user = 'leenarts-net'
  server = 'ssh.pcextreme.nl'
  path = 'domains/leenarts.net/htdocs/newblog'
  sh "rsync -rtzh --delete _site/ #{user}@#{server}:#{path}"
  puts 'Website published'
end